@using System.Configuration;
@using WhereAreThem.WebViewer.Models;

<!DOCTYPE html>
<html>
<head>
    <title>@ViewBag.Title</title>
    <link href="@Url.Content("~/Content/Site.css")" rel="stylesheet" type="text/css" />
    <script src="@Url.Content(ConfigurationManager.AppSettings["jquery"])" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.explorer.js")" type="text/javascript"></script>
</head>
<body>
    <div class="page">
        <div id="header">
            <div id="title">
                <h1>WhereAreThem</h1>
            </div>
            <div id="logindisplay">
                @(User == null ? string.Empty : User.Identity.Name)
            </div>
            <div id="menucontainer">
                <ul id="menu">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li><a class="changeView" href="#">Change View</a></li>
                </ul>
                <ul id="explorerViewMenu" class="contextMenu">
                    @foreach (ExplorerView view in Enum.GetValues(typeof(ExplorerView))) {
                        <li>
                            @Html.ActionLink(view.ToString(), "ChangeView", "Home", new { view = view }, null)
                        </li>
                    }
                </ul>
            </div>
        </div>
        <div id="main">
            @RenderBody()
        </div>
        <div id="footer">
        </div>
    </div>
</body>
</html>
